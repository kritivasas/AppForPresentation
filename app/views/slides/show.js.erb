$('#content_body').append("<%= raw(escape_javascript render :partial => 'show', :slide => @slide) %>");
$('.rest-in-place').restInPlace();
$("#preview_frame")[0].contentDocument.location.reload(true);
